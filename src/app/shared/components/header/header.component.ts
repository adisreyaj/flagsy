import { Component } from '@angular/core';

@Component({
  selector: `app-header`,
  template: `
    <header class="flex items-center justify-between px-4 py-2">Header</header>
  `,
  standalone: true,
})
export class HeaderComponent {}
